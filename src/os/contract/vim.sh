#!/usr/bin/env bash

cd "$(dirname "${BASH_SOURCE[0]}")" \
    && . "../utils.sh"

# - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - - -

nvim +PlugInstall +UpdateRemotePlugins +qa!
print_result $? "Install vim plugins"
